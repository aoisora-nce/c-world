#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

/**
 * Main entry point of the program.
 * 
 * @param argc The number of command-line arguments.
 * @param argv An array of string pointers representing the arguments.
 * @return 0 on successful execution, non-zero on failure.
 */
int main(int argc, char *argv[]) {
    // Prevent unused parameter compiler warnings if not using argc/argv
    (void)argc;
    (void)argv;

    // Your code goes here
    printf("Hello, World!\n");

    return 0;
}
